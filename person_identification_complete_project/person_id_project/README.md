
# Person Identification Clustering Project

Small end-to-end project to cluster unorganized face images by identity.

## Run
```bash
pip install -r requirements.txt
python src/pipeline.py --input_dir data --output_dir outputs
```

## Outputs
- `clusters.csv`
- `cluster_summary.csv`
- `clusters.json`
- `cluster_montage.png`
